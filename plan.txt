щоденник

1. шифрування
1.1 для кожного фалу свій пароль
1.2 (пароль як ключ для шифрування, дешифрування)/(пароль для доступу до файлу)?

2. читання/запис в 1 файл
2.1 (зміст, який знаходиться на початку файлу)/(в кінці файлу)/(розкиданий по файлу(після кожного розділу хедер з інфою про наступний розділ))
2.2 для змісту в цілому потрібна
    -дата, час
2.2.1 для розкиданого змісту потрібна
    -кількість байт, який займає наступний розділ
    -довгий запис в календар(прийдеться проходитись по всьому файлу)хотяя
2.2.2 для змісту на початку файлу -
    -залишити пам'ять для всього змісту(трабла, хз скільки виділити, пам'ять може закінчитись)
2.2.3 для змісту в кінці файлу
    -переміщати кожен раз весь зміст
    -адресу на початок кожного розділу
    -кількість байт для цего розділу
    +швидкий запис в календар
    -довгий запис в файл, якщо зміст великий

3. календар

4. гуї?

